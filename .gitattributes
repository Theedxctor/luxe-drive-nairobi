# Auto detect text files and normalize line endings
* text=auto eol=lf

# Explicitly handle Windows batch files
*.{cmd,[cC][mM][dD]} text eol=crlf
*.{bat,[bB][aA][tT]} text eol=crlf

# Denote all files that are truly binary and should not be modified
*.{png,jpg,jpeg,gif,ico,webp,woff,woff2,ttf,eot,svg,pdf} binary

# Force Unix line endings for shell scripts
*.sh text eol=lf

# Force Unix line endings for JS/TS files
*.{js,jsx,ts,tsx,json,md,html,css,scss} text eol=lf
